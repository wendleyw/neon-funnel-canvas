import React from 'react';
import { Handle, Position, NodeProps } from 'reactflow';

export const ComponentNodeConnectionPoints: React.FC = () => {
  // Removing side connection points - no longer needed
  return null;
};

// Styles for handles (can be customized or moved to a CSS file)
const handleStyle = {
  // ... existing code ...
};
